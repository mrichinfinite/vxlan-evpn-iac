---
- name: Deploy VXLAN EVPN L3 overlay
  hosts: leaves
  gather_facts: false

  vars_files:
    - ../data/standards.yml
    - ../data/fabric.yml
    - ../data/devices.yml
    - ../data/topology.yml

  roles:
    - role: 03_overlay_l3

  post_tasks:
    - name: Save running configuration to startup configuration
      cisco.nxos.nxos_command:
        commands:
          - copy running-config startup-config
      changed_when: false
