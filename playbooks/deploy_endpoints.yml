---
- name: Deploy endpoint attachments
  hosts: leaves
  gather_facts: false

  vars_files:
    - ../data/standards.yml
    - ../data/fabric.yml
    - ../data/devices.yml
    - ../data/topology.yml

  roles:
    - role: 06_endpoints
